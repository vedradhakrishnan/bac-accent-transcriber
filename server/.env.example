# Local-only mode
MODEL_PATH=./model                # where the model snapshot will live
PROCESSOR_ID=openai/whisper-small # keep in sync with your base model
PORT=8000
ALLOWED_ORIGINS=http://localhost:3000
# Hugging Face model to pull (repo id)
MODEL_SLUG=vedaradhak/bac-accent-transcriber
